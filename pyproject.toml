[project]
name = "bso-wholesale-integration"
authors = [
    { name = "Banu Saeed Organics" }
]
description = "Secure WordPress wholesale Lead ingestion for ERPNext 16"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0-dev"
erpnext = ">=16.0.0-dev"

